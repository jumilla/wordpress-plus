<?php

use Mockery as m;

class AdminRoutingTest extends TestCase
{
    public function testDashboard()
    {
        /*
        $this->putenvAndConfig('WP_BACKENDURL', 'wordpress.url.backend', 'http://localhost');
        $this->putenvAndConfig('WP_SITEURL', 'wordpress.url.site', 'http://localhost');

        $this->app = app();

        $this->visit('/wp-admin/');
        echo $this->response->getStatusCode();
*/
    }
}
