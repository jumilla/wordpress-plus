<?php

use Mockery as m;

class InstallCommandTest extends ConsoleCommandTestCase
{
    public function testRun()
    {
        /*		WP_Mock::wpFunction('wordpress_installed', [
            'times' => 1,
            'return' => false,
        ]);

        WP_Mock::wpPassthruFunction('wp_install', [
//			'times' => 1,
        ]);

        WP_Mock::wpPassthruFunction('update_options', [
//			'times' => 1,
        ]);
        wordpress_installed();
        return;*/

/*
        $command = new App\Console\Commands\WordPress\InstallCommand();

        $this->runCommand($command, [
        ]);*/
    }
}
